<fieldset data-payment-target="additionalFields" data-type="<%= @payment_types.where(name: "Purchase Order").first.id %>" >
  <div class="my-5">
    <%= form.label :po_number %>
    <%= form.number_field :po_number, class: "input-field" %>
  </div>
</fieldset>